{{#if useProps}}
import React from 'react'
import { Text } from 'react-native'
import { {{pascalCase name}}ScreenProps } from './types'
import * as S from './styles'

export const {{pascalCase name}}Screen = ({ }: {{pascalCase name}}ScreenProps) => {
    return (
        <S.{{pascalCase name}}>
            <Text accessibilityRole="text">{{pascalCase name}}</Text>
        </S.{{pascalCase name}}>
    )
}

{{else}}
import React from 'react'
import { Text } from 'react-native'
import * as S from './styles'

export const {{pascalCase name}}Screen = () => {
    return (
        <S.{{pascalCase name}}>
            <Text accessibilityRole="text">{{pascalCase name}}</Text>
        </S.{{pascalCase name}}>
    )
}

{{/if}}
