import { screen } from '@testing-library/react-native'
import { customRender } from '@/tests'
import { {{pascalCase name}}Screen } from '.'

describe('<{{pascalCase name}}Screen/>', () => {
    it('should render correctly', () => {
        customRender(<{{pascalCase name}}Screen/>)

        expect(screen.getByRole('text', { name: /{{pascalCase name}}/i })).toBeOnTheScreen()
    })
})
