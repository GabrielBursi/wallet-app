{{#if useProps}}
import React from 'react'
import { Text } from 'react-native'
import { {{pascalCase name}}ScreenProps } from './types'
import * as S from './styles'

const {{pascalCase name}}Screen = ({ }: {{pascalCase name}}ScreenProps) => {
    return (
        <S.{{pascalCase name}}>
            <Text>{{pascalCase name}}</Text>
        </S.{{pascalCase name}}>
    )
}

export default {{pascalCase name}}Screen
{{else}}
import React from 'react'
import { Text } from 'react-native'
import * as S from './styles'

const {{pascalCase name}}Screen = () => {
    return (
        <S.{{pascalCase name}}>
            <Text>{{pascalCase name}}</Text>
        </S.{{pascalCase name}}>
    )
}

export default {{pascalCase name}}Screen
{{/if}}